steps:
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    dir: 'api-gateway'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/${PROJECT_ID}/ragspace/api-gateway:${COMMIT_SHA}'
      - '-t'
      - 'gcr.io/${PROJECT_ID}/ragspace/api-gateway:latest'
      - '-f'
      - 'Dockerfile'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args:
      - 'push'
      - '--all-tags'
      - 'gcr.io/${PROJECT_ID}/ragspace/api-gateway'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-cloud-run'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'api-gateway'
      - '--image=gcr.io/${PROJECT_ID}/ragspace/api-gateway:${COMMIT_SHA}'
      - '--region=asia-south1'
      - '--platform=managed'

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/${PROJECT_ID}/ragspace/api-gateway:${COMMIT_SHA}'
  - 'gcr.io/${PROJECT_ID}/ragspace/api-gateway:latest'
