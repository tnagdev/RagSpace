steps:
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    dir: 'frontend'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/${PROJECT_ID}/ragspace/frontend:${COMMIT_SHA}'
      - '-t'
      - 'gcr.io/${PROJECT_ID}/ragspace/frontend:latest'
      - '-f'
      - 'Dockerfile'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args:
      - 'push'
      - '--all-tags'
      - 'gcr.io/${PROJECT_ID}/ragspace/frontend'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-cloud-run'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'frontend'
      - '--image=gcr.io/${PROJECT_ID}/ragspace/frontend:${COMMIT_SHA}'
      - '--region=asia-south1'
      - '--platform=managed'
      - '--allow-unauthenticated'

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/${PROJECT_ID}/ragspace/frontend:${COMMIT_SHA}'
  - 'gcr.io/${PROJECT_ID}/ragspace/frontend:latest'
