services:
  chroma:
    image: ghcr.io/chroma-core/chroma:latest
    container_name: chroma-file-embedder
    ports:
      - "8005:8000"
    volumes:
      - ./chroma_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=False
