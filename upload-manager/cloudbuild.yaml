steps:
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    dir: 'upload-manager'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/${PROJECT_ID}/ragspace/upload-manager:${COMMIT_SHA}'
      - '-t'
      - 'gcr.io/${PROJECT_ID}/ragspace/upload-manager:latest'
      - '-f'
      - 'Dockerfile'
      - '.'

  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args:
      - 'push'
      - '--all-tags'
      - 'gcr.io/${PROJECT_ID}/ragspace/upload-manager'

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-cloud-run'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'upload-manager'
      - '--image=gcr.io/${PROJECT_ID}/ragspace/upload-manager:${COMMIT_SHA}'
      - '--region=asia-south1'
      - '--platform=managed'
      - '--allow-unauthenticated'

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/${PROJECT_ID}/ragspace/upload-manager:${COMMIT_SHA}'
  - 'gcr.io/${PROJECT_ID}/ragspace/upload-manager:latest'
